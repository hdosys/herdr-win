From 047b50f27aabb7d0e73ba93b57efe28942c3378d Mon Sep 17 00:00:00 2001
From: User-3090 <71881005+User-3090@users.noreply.github.com>
Date: Thu, 27 Aug 2026 01:46:36 +0200
Subject: [PATCH 10/13] feat(agent): add completion alert controls

---
 docs/next/CHANGELOG.md                        |  2 +
 docs/next/api/herdr-api.schema.json           |  4 ++
 .../src/content/docs/configuration.mdx        |  9 +++--
 .../website/src/content/docs/socket-api.mdx   |  4 +-
 .../website/src/data/config-reference.json    |  6 +++
 src/api/schema/panes.rs                       |  3 ++
 src/app/actions.rs                            | 38 ++++++++++++-------
 src/app/api/panes.rs                          |  1 +
 src/app/api/plugins/mod.rs                    |  1 +
 src/cli/pane.rs                               |  1 +
 src/client/shell/config.rs                    |  3 ++
 src/client/shell/notification_policy.rs       | 27 ++++++++++++-
 src/client/shell/settings.rs                  | 13 ++++++-
 src/client/shell/settings_overlay.rs          | 12 ++++++
 src/client/shell/state.rs                     |  4 ++
 src/config/model.rs                           | 11 +++++-
 src/config/write.rs                           |  5 +++
 src/events.rs                                 |  1 +
 src/server/headless/tests/mod.rs              |  2 +
 19 files changed, 127 insertions(+), 20 deletions(-)

diff --git a/docs/next/CHANGELOG.md b/docs/next/CHANGELOG.md
index 6c69d7e51bfa5dc240cdd8e742868f4da09dc240..5fdf7cf00f59f8a082505b142605d1fb25f741df 100644
--- a/docs/next/CHANGELOG.md
+++ b/docs/next/CHANGELOG.md
@@ -8,8 +8,10 @@
 ### Changed
 - Pane and workspace metadata reports can atomically update and retain up to 64 custom tokens while preserving the existing key, value, source, and TTL bounds.
 - Herdr no longer queries all 256 indexed colors from the host terminal. It continues to synchronize the host foreground, background, and cursor while using its built-in indexed palette, preventing raw OSC 4 replies from reaching pane input. (#2786)
+- Agent completion alerts follow the upstream enabled default again and can be disabled from Settings > completion or with `ui.notify_on_agent_completion = false`; attention-required notifications remain enabled.
 
 ### Fixed
+- Cancelling an OpenCode response no longer reports an error that asks for attention.
 - Removing a background worktree workspace no longer changes focus to its parent workspace. (#3098)
 - Windows `agent start` now launches npm-installed agents with arguments through the interactive shell instead of passing a bare shim name to `Start-Process`. (#2685)
 - Full-lifecycle agent sessions now regain hook authority after a temporary foreground takeover ends instead of remaining permanently stale. (#1033)
diff --git a/docs/next/api/herdr-api.schema.json b/docs/next/api/herdr-api.schema.json
index 4532e918d04431e34413c14326df3ef1ef6755c9..64be90c2f9fa7d31b930685b7c8f6079605167a3 100644
--- a/docs/next/api/herdr-api.schema.json
+++ b/docs/next/api/herdr-api.schema.json
@@ -3020,6 +3020,10 @@
             },
             "state": {
               "$ref": "#/schemas/request/$defs/PaneAgentState"
+            },
+            "suppress_completion": {
+              "description": "Suppress completion attention when this report ends a user-cancelled operation.",
+              "type": "boolean"
             }
           },
           "required": [
diff --git a/docs/next/website/src/content/docs/configuration.mdx b/docs/next/website/src/content/docs/configuration.mdx
index 6fa1c411cb663f45f665406c26dc41ffac3f0a13..6922ca27a8d502795b948933d79996240f965dc8 100644
--- a/docs/next/website/src/content/docs/configuration.mdx
+++ b/docs/next/website/src/content/docs/configuration.mdx
@@ -470,9 +470,12 @@ Sidebar row settings affect only the expanded desktop sidebar. Collapsed and mob
 
 ## Notifications
 
-Herdr can notify you when a background agent finishes or needs input:
+Herdr notifies you when a background agent needs input or finishes. Completion alerts are enabled by default. Disable them from **Settings > completion** or in configuration when frequent idle transitions are not useful:
 
 ```toml
+[ui]
+notify_on_agent_completion = false
+
 [ui.toast]
 delivery = "herdr"
 delay_seconds = 1
@@ -481,7 +484,7 @@ delay_seconds = 1
 position = "bottom-right"
 ```
 
-Choose `herdr` for an in-app toast, `terminal` for an outer-terminal notification that also works over SSH, `system` for the local OS notification service, or `off` to disable popups. Herdr suppresses popups for the active tab. Search `ui.toast` in the [Config reference](/docs/config-reference/) for positions, delay behavior, and clipboard feedback settings.
+Choose `herdr` for an in-app toast, `terminal` for an outer-terminal notification that also works over SSH, `system` for the local OS notification service, or `off` to disable popups. Herdr suppresses popups for the active tab. `notify_on_agent_completion` affects only finished notifications, and user-cancelled responses never emit completion attention. Questions, permission prompts, and errors continue to request attention. Search `ui.toast` in the [Config reference](/docs/config-reference/) for positions, delay behavior, and clipboard feedback settings.
 
 On macOS, `system` tries `terminal-notifier` first and falls back to `/usr/bin/osascript` when it is unavailable or fails. The fallback appears as Script Editor in Notification Center and cannot activate the hosting terminal. Install `terminal-notifier` with `brew install terminal-notifier`. For a supported, detected terminal, it can activate the terminal app when you click the notification. Alternatively, choose `terminal` to let a supported outer terminal own the notification.
 
@@ -496,7 +499,7 @@ done_path = "sounds/done.mp3"
 request_path = "sounds/request.mp3"
 ```
 
-`path` sets one sound for all sound notifications. `done_path` and `request_path` override only the finished and needs-input sounds.
+`path` sets one sound for all sound notifications. `done_path` and `request_path` override only the finished and needs-input sounds. The done sound plays only when `ui.notify_on_agent_completion` is enabled.
 
 Per-agent sound overrides accept `default`, `on`, or `off`. Use detected agent labels such as `claude`, `codex`, `devin`, or `droid` as keys. Droid is muted by default.
 
diff --git a/docs/next/website/src/content/docs/socket-api.mdx b/docs/next/website/src/content/docs/socket-api.mdx
index 13a71abb9e39fb6ba2dd58e488eb679035d3a5ca..cbe70a70ae0c3d478a095163af879f06563cbbfc 100644
--- a/docs/next/website/src/content/docs/socket-api.mdx
+++ b/docs/next/website/src/content/docs/socket-api.mdx
@@ -711,7 +711,9 @@ Integrations report agent state with `pane.report_agent`.
 ```
 
 `state` carries semantic agent state and affects waits, notifications, and
-rollups. Report display-only values separately through metadata.
+rollups. Set `suppress_completion` only when an `idle` report ends a
+user-cancelled operation and must not request completion attention. Report
+display-only values separately through metadata.
 
 Session-only official integrations report native session references with `pane.report_agent_session`. State-reporting integrations can still include native session references in `pane.report_agent`. State-independent session reports do not affect waits, notifications, or rollups.
 
diff --git a/docs/next/website/src/data/config-reference.json b/docs/next/website/src/data/config-reference.json
index 8878b81af014cd72b4b9eb7ab643edd021c5751f..f93fc993eacc0818f3945dfa1bd3dbb0331022e7 100644
--- a/docs/next/website/src/data/config-reference.json
+++ b/docs/next/website/src/data/config-reference.json
@@ -1080,6 +1080,12 @@
       "id": "notifications",
       "title": "Notifications",
       "keys": [
+        {
+          "key": "ui.notify_on_agent_completion",
+          "type": "boolean",
+          "default": "true",
+          "description": "Notify when a background agent finishes. Disable from Settings > completion or set false to hide completion popups and done sounds. Questions, permission prompts, and errors remain actionable regardless of this setting."
+        },
         {
           "key": "ui.toast.delivery",
           "type": "enum",
diff --git a/src/api/schema/panes.rs b/src/api/schema/panes.rs
index 517870f771a4340649850613551a470384597014..3df9d25311295a59c1ea536282da9daf6df8a4e2 100644
--- a/src/api/schema/panes.rs
+++ b/src/api/schema/panes.rs
@@ -458,6 +458,9 @@ pub struct PaneReportAgentParams {
     pub agent_session_id: Option<String>,
     #[serde(default, skip_serializing_if = "Option::is_none")]
     pub agent_session_path: Option<String>,
+    /// Suppress completion attention when this report ends a user-cancelled operation.
+    #[serde(default, skip_serializing_if = "super::is_false")]
+    pub suppress_completion: bool,
 }
 
 #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, schemars::JsonSchema)]
diff --git a/src/app/actions.rs b/src/app/actions.rs
index ca6f67046fde1c39805f1a85828b148320e3e32d..752d6829d41629b8faa6442128c3cf1436dfe7ee 100644
--- a/src/app/actions.rs
+++ b/src/app/actions.rs
@@ -1674,6 +1674,7 @@ impl AppState {
                 message,
                 seq,
                 session_ref,
+                suppress_completion,
             } => {
                 if crate::agent_resume::is_reserved_native_state_source(&source, &agent_label) {
                     self.update_terminal_state(pane_id, |terminal| {
@@ -1682,16 +1683,20 @@ impl AppState {
                     .into_iter()
                     .collect()
                 } else {
-                    self.update_terminal_state(pane_id, |terminal| {
-                        terminal.set_hook_authority_with_session_ref(
-                            source,
-                            agent_label,
-                            state,
-                            message,
-                            session_ref,
-                            seq,
-                        )
-                    })
+                    self.update_terminal_state_with_completion_policy(
+                        pane_id,
+                        suppress_completion,
+                        |terminal| {
+                            terminal.set_hook_authority_with_session_ref(
+                                source,
+                                agent_label,
+                                state,
+                                message,
+                                session_ref,
+                                seq,
+                            )
+                        },
+                    )
                     .into_iter()
                     .collect()
                 }
@@ -1868,9 +1873,10 @@ impl AppState {
         }
         let agent_released = mutation.agent_released;
         let change = mutation.effective_state_change.or(unchanged_change)?;
-        let suppress_completion = force_suppress_completion
-            || (change.state == AgentState::Idle
-                && (managed_launch_pending || suppress_acquisition_completion));
+        let suppress_completion = change.state == AgentState::Idle
+            && (force_suppress_completion
+                || managed_launch_pending
+                || suppress_acquisition_completion);
         if change.previous_state != change.state {
             self.next_agent_state_change_seq += 1;
             if let Some(terminal) = self.terminals.get_mut(&terminal_id) {
@@ -3314,6 +3320,7 @@ mod tests {
         let bg_pane_id = *state.workspaces[1].panes.keys().next().unwrap();
 
         state.handle_app_event(AppEvent::HookStateReported {
+            suppress_completion: false,
             pane_id: bg_pane_id,
             source: "custom:hermes".into(),
             agent_label: "hermes".into(),
@@ -3352,6 +3359,7 @@ mod tests {
             observed_at: std::time::Instant::now(),
         });
         state.handle_app_event(AppEvent::HookStateReported {
+            suppress_completion: false,
             pane_id: bg_pane_id,
             source: "herdr:codex".into(),
             agent_label: "codex".into(),
@@ -3400,6 +3408,7 @@ mod tests {
             observed_at: std::time::Instant::now(),
         });
         state.handle_app_event(AppEvent::HookStateReported {
+            suppress_completion: false,
             pane_id,
             source: "herdr:claude".into(),
             agent_label: "claude".into(),
@@ -3509,6 +3518,7 @@ mod tests {
             observed_at: std::time::Instant::now(),
         });
         state.handle_app_event(AppEvent::HookStateReported {
+            suppress_completion: false,
             pane_id,
             source: "herdr:devin".into(),
             agent_label: "devin".into(),
@@ -3533,6 +3543,7 @@ mod tests {
         let second_session = test_dir.join("two.jsonl").display().to_string();
 
         let first_updates = state.handle_app_event(AppEvent::HookStateReported {
+            suppress_completion: false,
             pane_id,
             source: "custom:pi".into(),
             agent_label: "pi".into(),
@@ -3545,6 +3556,7 @@ mod tests {
         state.session_dirty = false;
 
         let second_updates = state.handle_app_event(AppEvent::HookStateReported {
+            suppress_completion: false,
             pane_id,
             source: "custom:pi".into(),
             agent_label: "pi".into(),
diff --git a/src/app/api/panes.rs b/src/app/api/panes.rs
index e71ce94f7e08ab36d5b6ceacb2e17b257f239bcd..3e4d63c24e884929c31fa048dde22b483dc434b1 100644
--- a/src/app/api/panes.rs
+++ b/src/app/api/panes.rs
@@ -1552,6 +1552,7 @@ impl App {
             state: detect_state_from_api(params.state),
             message: params.message,
             seq: params.seq,
+            suppress_completion: params.suppress_completion,
         });
 
         encode_success(id, ResponseResult::Ok {})
diff --git a/src/app/api/plugins/mod.rs b/src/app/api/plugins/mod.rs
index 8ad083d43f0e3c5e96e43f1ee9bb3f54af45e340..f3b348dd63b35caf7c56c3822c8c2f56a51600e9 100644
--- a/src/app/api/plugins/mod.rs
+++ b/src/app/api/plugins/mod.rs
@@ -3125,6 +3125,7 @@ action = "missing"
         let _ = app.handle_pane_report_agent(
             "report".into(),
             crate::api::schema::PaneReportAgentParams {
+                suppress_completion: false,
                 pane_id: pane_public.clone(),
                 source: "test".into(),
                 agent: "codex".into(),
diff --git a/src/cli/pane.rs b/src/cli/pane.rs
index 17d3c144b43102c19206f1a203d4c1296a3ba215..e3d636592661327160b84d5b6c29ff72cf14bd91 100644
--- a/src/cli/pane.rs
+++ b/src/cli/pane.rs
@@ -1290,6 +1290,7 @@ fn pane_report_agent(args: &[String]) -> std::io::Result<i32> {
         seq,
         agent_session_id,
         agent_session_path,
+        suppress_completion: false,
     }))
 }
 
diff --git a/src/client/shell/config.rs b/src/client/shell/config.rs
index e8a9b4bb8694357e2f74088f436ea63df4389f38..c2f851639fffb33d8ad4c76f1092eaf656f65c08 100644
--- a/src/client/shell/config.rs
+++ b/src/client/shell/config.rs
@@ -60,6 +60,7 @@ impl ClientShellState {
                     &loaded.diagnostics,
                     &loaded.invalid_sections,
                 );
+                self.discard_disabled_completions(std::time::Instant::now());
                 if let Some(appearance) = self.host_appearance {
                     self.config.palette = crate::app::client_palette_for_appearance(
                         &self.config.theme_runtime,
@@ -110,6 +111,7 @@ impl ClientShellConfig {
             status_indicators: config.ui.status_indicators,
             sound_enabled: config.ui.sound.enabled,
             toast_delivery: config.ui.toast.delivery,
+            notify_on_agent_completion: config.ui.notify_on_agent_completion,
             toast_delay_seconds: config.ui.toast.delay_seconds,
             toast_position: config.ui.toast.herdr.position,
             copy_on_select: config.ui.copy_on_select,
@@ -312,6 +314,7 @@ impl ClientShellConfig {
                 self.status_indicators = ui.status_indicators;
                 self.sound_enabled = ui.sound.enabled;
                 self.toast_delivery = ui.toast.delivery;
+                self.notify_on_agent_completion = ui.notify_on_agent_completion;
                 self.toast_delay_seconds = ui.toast.delay_seconds;
                 self.toast_position = ui.toast.herdr.position;
                 self.copy_on_select = ui.copy_on_select;
diff --git a/src/client/shell/notification_policy.rs b/src/client/shell/notification_policy.rs
index f3eeeb3c4f50fb54eb5259e7fa7984c3df47e207..f4ca29941e09abe00cfebc771b42c93a09c95891 100644
--- a/src/client/shell/notification_policy.rs
+++ b/src/client/shell/notification_policy.rs
@@ -102,6 +102,11 @@ impl ClientShellState {
         event: SemanticNotification,
         now: std::time::Instant,
     ) -> (Vec<ClientShellNotificationEffect>, bool) {
+        if event.kind == SemanticNotificationKind::Finished
+            && !self.config.notify_on_agent_completion
+        {
+            return (Vec::new(), false);
+        }
         let delay = if event.kind == SemanticNotificationKind::Custom {
             0
         } else {
@@ -144,11 +149,31 @@ impl ClientShellState {
         (effects, repaint || cleared_visible)
     }
 
+    pub(super) fn discard_disabled_completions(&mut self, now: std::time::Instant) -> bool {
+        let mut repaint = false;
+        if !self.config.notify_on_agent_completion {
+            self.pending_notifications
+                .retain(|pending| pending.event.kind != SemanticNotificationKind::Finished);
+            self.queued_notifications
+                .retain(|queued| queued.event.kind != SemanticNotificationKind::Finished);
+            if self
+                .visible_notification
+                .as_ref()
+                .is_some_and(|visible| visible.event.kind == SemanticNotificationKind::Finished)
+            {
+                self.visible_notification = None;
+                self.promote_queued_notification(now);
+                repaint = true;
+            }
+        }
+        repaint
+    }
+
     pub(crate) fn tick_notifications(
         &mut self,
         now: std::time::Instant,
     ) -> (Vec<ClientShellNotificationEffect>, bool) {
-        let mut repaint = false;
+        let mut repaint = self.discard_disabled_completions(now);
         if self
             .visible_notification
             .as_ref()
diff --git a/src/client/shell/settings.rs b/src/client/shell/settings.rs
index 200201c5d6bedaaa2a473dae4d38a1072bd149ab..869c4f4dc0cafda16351bca7a366c55417092278 100644
--- a/src/client/shell/settings.rs
+++ b/src/client/shell/settings.rs
@@ -50,6 +50,9 @@ impl ClientShellState {
             ClientSettingsSection::Theme => theme_index(&self.config.theme_name),
             ClientSettingsSection::Indicators => indicator_index(self.config.status_indicators),
             ClientSettingsSection::Sound => usize::from(!self.config.sound_enabled),
+            ClientSettingsSection::Completion => {
+                usize::from(!self.config.notify_on_agent_completion)
+            }
             ClientSettingsSection::Toast => toast_index(self.config.toast_delivery),
             ClientSettingsSection::Integrations => 0,
         }
@@ -97,7 +100,9 @@ impl ClientShellState {
         match self.overlay.as_ref() {
             Some(ClientShellOverlay::Settings(settings)) => match settings.section {
                 ClientSettingsSection::Theme => crate::config::THEME_NAMES.len(),
-                ClientSettingsSection::Indicators | ClientSettingsSection::Sound => 2,
+                ClientSettingsSection::Indicators
+                | ClientSettingsSection::Sound
+                | ClientSettingsSection::Completion => 2,
                 ClientSettingsSection::Toast => 4,
                 ClientSettingsSection::Integrations => settings.integrations.len(),
             },
@@ -210,6 +215,12 @@ impl ClientShellState {
             ClientSettingsSection::Sound => {
                 self.save_settings_edit(crate::config::ConfigEdit::Sound(selected == 0), outcome);
             }
+            ClientSettingsSection::Completion => {
+                self.save_settings_edit(
+                    crate::config::ConfigEdit::CompletionNotifications(selected == 0),
+                    outcome,
+                );
+            }
             ClientSettingsSection::Toast => {
                 let delivery = match selected {
                     0 => crate::config::ToastDelivery::Off,
diff --git a/src/client/shell/settings_overlay.rs b/src/client/shell/settings_overlay.rs
index ea59ad02971ecd3d75215c82233ad5e2bf3e6b24..8df82383b0c0f9001c3facb2e2bb97827de0a13b 100644
--- a/src/client/shell/settings_overlay.rs
+++ b/src/client/shell/settings_overlay.rs
@@ -194,6 +194,18 @@ pub(super) fn render_settings_overlay(
                 &mut choice_hits,
             );
         }
+        ClientSettingsSection::Completion => {
+            render_choice_section(
+                buffer,
+                content,
+                "agent completion",
+                "show completion popups and play done sounds",
+                &["on", "off"],
+                settings.selected,
+                palette,
+                &mut choice_hits,
+            );
+        }
         ClientSettingsSection::Integrations => {
             render_integrations(buffer, content, settings, palette);
         }
diff --git a/src/client/shell/state.rs b/src/client/shell/state.rs
index 9de144f547fe2e3fa79c9104eab1cc11b4b2cd57..648f01708b8f83320f0535b44b736886dd22ed01 100644
--- a/src/client/shell/state.rs
+++ b/src/client/shell/state.rs
@@ -83,6 +83,7 @@ pub(crate) struct ClientShellConfig {
     pub(super) status_indicators: crate::config::StatusIndicatorStyle,
     pub(super) sound_enabled: bool,
     pub(super) toast_delivery: crate::config::ToastDelivery,
+    pub(super) notify_on_agent_completion: bool,
     pub(super) toast_delay_seconds: u64,
     pub(super) toast_position: crate::config::ToastHerdrPosition,
     pub(super) copy_on_select: bool,
@@ -441,6 +442,7 @@ pub(super) enum ClientSettingsSection {
     Theme,
     Indicators,
     Sound,
+    Completion,
     Toast,
     Integrations,
 }
@@ -450,6 +452,7 @@ impl ClientSettingsSection {
         Self::Theme,
         Self::Indicators,
         Self::Sound,
+        Self::Completion,
         Self::Toast,
         Self::Integrations,
     ];
@@ -459,6 +462,7 @@ impl ClientSettingsSection {
             Self::Theme => "theme",
             Self::Indicators => "indicators",
             Self::Sound => "sound",
+            Self::Completion => "completion",
             Self::Toast => "toasts",
             Self::Integrations => "integrations",
         }
diff --git a/src/config/model.rs b/src/config/model.rs
index d9f8acbe09408cea8998aa9712161247eb28615a..754a4ad7e19e0f36fd99418ec489637c11a0c1b3 100644
--- a/src/config/model.rs
+++ b/src/config/model.rs
@@ -940,6 +940,9 @@ pub struct UiConfig {
     /// Accent color for highlights, borders, and navigation UI.
     /// Accepts hex (#89b4fa), named colors (cyan, blue), or RGB (rgb(137,180,250)).
     pub accent: String,
+    /// Notify when a background agent finishes. Default: true.
+    /// Attention-required notifications are unaffected.
+    pub notify_on_agent_completion: bool,
     /// Optional visual toast notifications for background workspace events.
     pub toast: ToastConfig,
     /// Play sounds when agents change state in background workspaces.
@@ -1161,6 +1164,7 @@ impl Default for UiConfig {
             status_indicators: StatusIndicatorStyle::Dots,
             sidebar: SidebarConfig::default(),
             accent: "cyan".into(),
+            notify_on_agent_completion: true,
             toast: ToastConfig::default(),
             sound: SoundConfig::default(),
         }
@@ -1783,6 +1787,9 @@ mouse_scroll_lines = 0
     #[test]
     fn toast_config_parses() {
         let toml = r#"
+[ui]
+notify_on_agent_completion = true
+
 [ui.toast]
 delivery = "terminal"
 delay_seconds = 2
@@ -1795,6 +1802,7 @@ enabled = false
 position = "top-center"
 "#;
         let config: Config = toml::from_str(toml).unwrap();
+        assert!(config.ui.notify_on_agent_completion);
         assert_eq!(config.ui.toast.delivery, ToastDelivery::Terminal);
         assert_eq!(config.ui.toast.delay_seconds, 2);
         assert_eq!(config.ui.toast.herdr.position, ToastHerdrPosition::TopLeft);
@@ -1806,8 +1814,9 @@ position = "top-center"
     }
 
     #[test]
-    fn toast_config_defaults_preserve_existing_behavior_with_delay() {
+    fn notification_defaults_enable_completion_with_delay() {
         let config = Config::default();
+        assert!(config.ui.notify_on_agent_completion);
         assert_eq!(config.ui.toast.delivery, ToastDelivery::Off);
         assert_eq!(config.ui.toast.delay_seconds, 1);
         assert_eq!(
diff --git a/src/config/write.rs b/src/config/write.rs
index 752445831f0056ddc10b9d9d1b2b92d5eab29616..ef4f089b3315ce3b41feed4524166fba65b192f1 100644
--- a/src/config/write.rs
+++ b/src/config/write.rs
@@ -3,6 +3,7 @@ pub(crate) enum ConfigEdit<'a> {
     Theme(&'a str),
     StatusIndicators(super::StatusIndicatorStyle),
     Sound(bool),
+    CompletionNotifications(bool),
     ToastDelivery(super::ToastDelivery),
 }
 
@@ -12,6 +13,7 @@ impl ConfigEdit<'_> {
             Self::Theme(_) => "theme",
             Self::StatusIndicators(_) => "status indicators",
             Self::Sound(_) => "sound setting",
+            Self::CompletionNotifications(_) => "completion notifications",
             Self::ToastDelivery(_) => "toast setting",
         }
     }
@@ -32,6 +34,9 @@ impl ConfigEdit<'_> {
             Self::Sound(enabled) => {
                 super::upsert_section_bool(content, "ui.sound", "enabled", enabled)
             }
+            Self::CompletionNotifications(enabled) => {
+                super::upsert_section_bool(content, "ui", "notify_on_agent_completion", enabled)
+            }
             Self::ToastDelivery(delivery) => {
                 let value = match delivery {
                     super::ToastDelivery::Off => "\"off\"",
diff --git a/src/events.rs b/src/events.rs
index 956389912a03bb6143d1299c536c2e4f37920185..3f1ed53b18d9f56bcc6f441f856fcd4bd03fae03 100644
--- a/src/events.rs
+++ b/src/events.rs
@@ -87,6 +87,7 @@ pub enum AppEvent {
         message: Option<String>,
         seq: Option<u64>,
         session_ref: Option<crate::agent_resume::AgentSessionRef>,
+        suppress_completion: bool,
     },
     /// Agent session identity was reported without state authority.
     AgentSessionReported {
diff --git a/src/server/headless/tests/mod.rs b/src/server/headless/tests/mod.rs
index 9c148a162cd7cf0fecc8e782dd26b27d83757664..d844e3c09bca7e68d5b2db3d2d7afd39eaea64b1 100644
--- a/src/server/headless/tests/mod.rs
+++ b/src/server/headless/tests/mod.rs
@@ -4656,6 +4656,7 @@ fn headless_scheduled_tasks_expire_agent_metadata() {
 
     assert!(
         server.handle_internal_event_with_forwarding(AppEvent::HookStateReported {
+            suppress_completion: false,
             pane_id,
             source: "custom:pi".into(),
             agent_label: "pi".into(),
@@ -6354,6 +6355,7 @@ fn stale_api_agent_report_does_not_forward_done_sound() {
         request: api::schema::Request {
             id: "stale".into(),
             method: api::schema::Method::PaneReportAgent(api::schema::PaneReportAgentParams {
+                suppress_completion: false,
                 pane_id: public_pane_id,
                 source: "herdr:pi".into(),
                 agent: "pi".into(),
-- 
2.55.0.windows.3

